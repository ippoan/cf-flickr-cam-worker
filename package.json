{
  "name": "cf-flickr-cam-worker",
  "version": "0.1.0",
  "private": true,
  "description": "Cron trigger + thin proxy to ohishi-logi (Cloud Run). Flickr OAuth1.0a authorization + multipart upload + access token persistence (KV).",
  "scripts": {
    "dev": "wrangler dev",
    "deploy": "wrangler deploy",
    "test": "vitest run",
    "test:watch": "vitest",
    "typecheck": "tsc --noEmit"
  },
  "devDependencies": {
    "@cloudflare/workers-types": "^5.20260706.1",
    "@vitest/coverage-v8": "^3.2.4",
    "typescript": "^5.8.3",
    "vitest": "^3.2.4",
    "wrangler": "^4.67.0"
  }
}
