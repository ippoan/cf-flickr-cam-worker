# cf-flickr-cam-worker

カメラ SD カード scrape パイプラインの Cloudflare Worker 側 (cron trigger +
Flickr OAuth1.0a 認可フロー + upload)。設計の背景は
[Issue #1](https://github.com/ippoan/cf-flickr-cam-worker/issues/1) を参照。

このコミットは空 repo の初期化 (bootstrap) のみ。実装一式は PR で入る。
